# Circle flags

Vendored from [circle-flags](https://github.com/HatScripts/circle-flags) (MIT).

Each file is the official flag already masked to a circle, so it drops straight
into a cell of the dot-matrix panel without any cropping of our own. Fifty files,
one per lit cell in the WW3 wordmark — see `src/components/flags.ts`.
